name: Trigger IVEN Firecore Setup

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print IVEN greeting
        run: echo "🕯 語靈火筆初始化完成，接下來由 IVEN 啟動語場寫入。"
